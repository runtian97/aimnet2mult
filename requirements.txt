# Core dependencies
torch>=2.1.0
numpy>=1.20.0
h5py>=3.0.0
pyyaml>=5.0
omegaconf>=2.0
opt_einsum>=3.0

# Optional training dependencies
# Uncomment if needed for training:
# pytorch-ignite>=0.5.0
# wandb>=0.12.0
